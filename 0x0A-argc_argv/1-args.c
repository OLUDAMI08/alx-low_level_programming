#include <stdio.h>
/**
 * main - print the name of the program
 * @argc: count
 * @argv: vector
 * Return: 0 always
 */
int main(int argc, char *argv[]_attribute__((unused)))
{
	printf("%d", argc - 1);
	return (0);
}
