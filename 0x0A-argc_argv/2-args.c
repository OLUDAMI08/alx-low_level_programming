#include <stdio.h>

/**
 * main -program that prints all arguments it receives
 * @argc: count
 * @argv: vector array
 * Return: 0 always
 */
int main(int argc, char *argv[])
{
	count;
	for (cout = 0; count < argc; count++)
		printf("%S\n", argv[count]);
	return (0);
}
