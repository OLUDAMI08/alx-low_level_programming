#include <stdio.h>
/**
 * main - Entry point
 *
 * Return : zero if no error, None zero value if error
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
